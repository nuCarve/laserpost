{
  "setups": {
    "default": {
      "posts": ["laserpost-lightburn", "laserpost-svg"],
      "options": {
        "quiet": "--quiet",
        "nobackup": "--nobackup",
        "noeditor": "--noeditor",
        "nointeraction": "--nointeraction",
        "timeout": ["--timeout", "1"]
      },
      "cnc": "test"
    },
    "defaultBasic": {
      "setup": "default",
      "properties": {
        "work0100TraceStock": "0"
      }
    },
    "stockTrace": {
      "setup": "defaultBasic",
      "properties": {
        "work0100TraceStock": "1"
      }
    },
    "workOffset": {
      "setup": "defaultBasic",
      "options": {
        "nobackup": null
      },
      "properties": {
        "work0200OffsetX": "100",
        "work0300OffsetY": "200"
      }
    }
  },
  "tests": [
    {
      "name": "Default basic test",
      "setup": "default"
    },
    {
      "name": "Trace stock LBRN only",
      "setup": "workOffset",
      "properties": {
        "work0100TraceStock": "1"
      },
      "posts": ["laserpost-lightburn"],
      "result-as-single": "compare|fail|pass|match:[something]",
      "result-as-arrray": ["compare", "warning", "match:pattern"]
    }
  ]
}
