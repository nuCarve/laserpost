{
  "setups": {
    "default": {
      "posts": ["laserpost-lightburn", "laserpost-svg"],
      "options": {
        "quiet": "--quiet",
        "nobackup": "--nobackup",
        "noeditor": "--noeditor",
        "nointeraction": "--nointeraction",
        "timeout": ["--timeout", "1"]
      },
      "cnc": "cover",
      "validate": [
        { "post": "laserpost-lightburn", "file": "cover-1.xml", "elements": ["LightBurnProject.Shape[*]", "LightBurnProject.CutSetting[*]"] },
        { "post": "laserpost-svg", "file": "cover-1.svg", "elements": ["svg[1] (no children?)"]},
        { "comment": "// also, how do we modify these rules in inheritance?" },
        { "comment": "// do we want log file / non-XML files (set up the setup.txt file)?" }
      ],
      "properties": {
        "work0100TraceStock": "0"
      }
    }
  },
  "tests": [
    {
      "name": "Default parameters cover",
      "setup": "default"
    },
    {
      "name": "Trace stock cover",
      "setup": "default",
      "properties": {
        "work0100TraceStock": "1"
      }
    },
    {
      "name": "Work offset cover",
      "setup": "default",
      "properties": {
        "work0200OffsetX": "100",
        "work0300OffsetY": "200"
      }
      }
  ]
}
